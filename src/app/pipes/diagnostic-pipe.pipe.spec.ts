import { DiagnosticPipePipe } from './diagnostic-pipe.pipe';

describe('DiagnosticPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DiagnosticPipePipe();
    expect(pipe).toBeTruthy();
  });
});
