import { DiseaseSymptomsPipe } from './disease-symptoms.pipe';

describe('DiseaseSymptomsPipe', () => {
  it('create an instance', () => {
    const pipe = new DiseaseSymptomsPipe();
    expect(pipe).toBeTruthy();
  });
});
