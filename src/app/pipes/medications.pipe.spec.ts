import { MedicationsPipe } from './medications.pipe';

describe('MedicationsPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicationsPipe();
    expect(pipe).toBeTruthy();
  });
});
