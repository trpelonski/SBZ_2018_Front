import { SubstancesPipe } from './substances.pipe';

describe('SubstancesPipe', () => {
  it('create an instance', () => {
    const pipe = new SubstancesPipe();
    expect(pipe).toBeTruthy();
  });
});
